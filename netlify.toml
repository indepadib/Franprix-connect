[build]
  command = "npm run build"
  publish = "dist"

# Si Netlify s'obstine avec Next.js, on peut essayer d'ignorer les plugins
# via la configuration locale, mais le plus sûr est de le désactiver dans l'interface Netlify.
